# GameRules.BooleanValue.create(boolean) is private on this Minecraft version, and unlike
# NeoForge / Forge 1.20.1 the Forge access transformer for this version does not widen it.
# Registering the mod's per-world starter house game rule needs a boolean rule type built
# from a config-driven default, so open it up here.
#
# Written with SRG names (GameRules.BooleanValue.create -> GameRules$BooleanValue.func_223568_b,
# returning GameRules$RuleType) because Forge applies this file at runtime, where 1.16.5 is
# SRG-named for classes as well as members; Architectury Loom remaps it for the dev
# environment, so one file serves both.
public net.minecraft.world.GameRules$BooleanValue func_223568_b(Z)Lnet/minecraft/world/GameRules$RuleType;
