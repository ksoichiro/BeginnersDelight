# GameRules.BooleanValue.create(boolean) is package-private on this Minecraft version, and
# unlike NeoForge / Forge 1.20.1 the Forge access transformer for this version does not widen
# it. Registering the mod's per-world starter house game rule needs a boolean rule type built
# from a config-driven default, so open it up here.
#
# Written with the SRG member name (create -> m_46250_) because Forge applies this file at
# runtime, where members are SRG-named; Architectury Loom remaps it for the dev environment,
# so one file serves both.
public net.minecraft.world.level.GameRules$BooleanValue m_46250_(Z)Lnet/minecraft/world/level/GameRules$Type;
